# to use wikipedia

# firstly, install wikipedia module
# pip install wikipedia

# importing wikipedia
import wikipedia

# write the below snippet for wikipedia search

# if "wikipedia" in query:
#     speak("Searching...")
#     query = query.replace("wikipedia", "")
#     result = wikipedia.summary(query, sentences = 2)
#     speak(result)
        